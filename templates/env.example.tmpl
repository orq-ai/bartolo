# Export these variables in your shell.
# {{ .CommandName }} does not read .env files unless you opt in per invocation
# with {{ .NameEnv }}_DOTENV=1, and even then it imports only its own variables.
{{ .APIKeyEnvVar }}=replace-me
