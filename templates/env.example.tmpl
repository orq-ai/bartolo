# Copy this file to .env or export the variables in your shell.
# .env files are only read when dotenv loading is enabled: set
# {{ .NameEnv }}_DOTENV=1 or put "dotenv": true in the config file.
{{ .APIKeyEnvVar }}=replace-me
