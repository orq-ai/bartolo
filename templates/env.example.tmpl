# Export these variables in your shell.
# {{ .CommandName }} does not read .env files unless you opt in by running a
# command with {{ .NameEnv }}_DOTENV=1, and even then it imports only its own
# {{ .NameEnv }}_ variables.
{{ .APIKeyEnvVar }}=replace-me
